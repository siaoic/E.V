# MaiBot → TypeScript 迁移前置调研

> 项目版本：1.2.5 ｜ 声明 Python 要求：>=3.12 ｜ 调研日期：2026-09-18
> 调研范围：本仓库（`e:\AI\直播`）实际代码，不含推测。

## 结论速览

| 维度 | 结论 | 迁移难度 |
| --- | --- | --- |
| 依赖栈 | 纯 Python + 少量 Node（Electron 前端）；无 Docker | 低 |
| 关系库 | SQLite + SQLModel(SQLAlchemy)，50+ 表 | 中 |
| 向量库 | FAISS 本地索引（另有 SQLite 内嵌向量路径） | **高** |
| Embedding | `sentence-transformers` 本地模型 | **高** |
| LLM | OpenAI 兼容 HTTP + Gemini SDK | 低 |
| TTS | 本地 GSV-TTS-Lite（torch），SSE + `sounddevice` 播放 | **高** |
| 插件系统 | 独立子进程 + RPC（Named Pipe / UDS / TCP） | 中（协议可复用） |
| 后台任务 | 纯 `asyncio.create_task` + 内存态 job dict | 中（多实例不兼容） |
| WebSocket | FastAPI 原生 WS + domain:topic 订阅模型 | 低 |
| 前端 | 在本仓库 `dashboard/`，已全面使用 `/api/webui/*` | 低 |

**核心判断**：接口层、鉴权、WebSocket、插件 RPC 协议都是**可直译**的；真正的硬阻塞只有三块——**FAISS 向量检索**、**sentence-transformers 嵌入**、**GSV-TTS-Lite 本地语音合成**。这三块建议保留 Python 侧不动，其余可 TS 重写。

---

# 一、必须有（缺了没法动）

## 1. 依赖清单

### 1.1 requirements.txt（完整，未删减）

```text
Babel>=2.17.0
ahocorasick-rs>=1.0.3
aiohttp>=3.14.3
certifi
colorama>=0.4.6
faiss-cpu>=1.11.0
fastapi>=0.116.0
google-genai>=1.39.1
httpx[socks]
jieba>=0.42.1
json-repair>=0.60.1,<0.61
maim-message==0.6.8
maibot-dashboard==1.7.4
maibot-plugin-sdk>=2.8.1
mcp>=1.28.1,<2
msgpack>=1.2.1
numpy>=2.2.6
openai>=1.95.0
pandas>=2.3.1
pillow>=12.3.0
playwright>=1.54.0
pyarrow>=20.0.0
pydantic>=2.11.7
pypinyin>=0.54.0
prompt-toolkit>=3.0.52
python-multipart>=0.0.31
rapidfuzz
rich>=14.0.0
scipy>=1.7.0
sqlalchemy>=2.0.40
sqlmodel>=0.0.24
sounddevice>=0.5.1
soundfile>=0.14.0
structlog>=25.4.0
tomlkit>=0.13.3
typing-extensions
uvicorn>=0.35.0
watchfiles>=1.1.1
```

### 1.2 pyproject.toml 差异（requirements.txt 未包含）

```text
blivedm @ git+https://github.com/xfgryujk/blivedm
brotlicffi>=0.9.0
```

### 1.3 开发依赖

```text
pytest
pytest-asyncio
ruff>=0.12.2
zstandard
```

### 1.4 子服务独立依赖

**`tts_service/requirements.txt`**（本地语音合成服务，独立进程）

```text
fastapi>=0.110
uvicorn>=0.29
numpy
soundfile
gsv-tts-lite>=0.4.7
# 需按 GSV-TTS-Lite 官方说明额外安装 torch 及推理后端（CUDA / MPS / CPU）
```

**`src/A_memorix/requirements.txt`**（长期记忆插件）

```text
numpy>=1.20.0
scipy>=1.7.0
networkx>=3.0.0
pyarrow>=10.0.0
pandas>=1.5.0
nest-asyncio>=1.5.0
faiss-cpu>=1.7.0
uvicorn>=0.20.0
fastapi>=0.100.0
pydantic>=2.0.0
python-multipart>=0.0.9
rich>=14.0.0
tenacity>=8.0.0
jieba>=0.42.1
```

### 1.5 依赖 → TS 替换对照

| Python | TS 侧方案 | 备注 |
| --- | --- | --- |
| fastapi | Fastify | 路由/依赖注入模式差异大，需重写中间件 |
| pydantic / sqlmodel | zod | 声明式校验可直译；`table=True` 需拆成 zod + ORM schema |
| sqlalchemy / sqlmodel | Drizzle 或 Prisma | **SQLite 优先选 Drizzle**（更贴近裸 SQL，便于复用现有迁移 SQL） |
| uvicorn | 无需替代 | 由 Fastify 自带 |
| openai | `openai` npm 官方 SDK | 基本平替 |
| google-genai | `@google/genai` | 平替 |
| mcp | `@modelcontextprotocol/sdk` | 平替 |
| aiohttp / httpx | 原生 `fetch` / undici | 平替 |
| structlog | pino | 平替 |
| tomlkit | `smol-toml` / `@iarna/toml` | 需保留「保留注释」能力选 smol-toml |
| watchfiles | `chokidar` | 平替 |
| jieba / pypinyin / rapidfuzz / ahocorasick-rs | `nodejieba` / `pinyin` / `fuse.js` | **属中文分词与模糊匹配，行为需对齐测试** |
| faiss-cpu | ❌ 无成熟平替 | **保留 Python 或换向量库（改语义）** |
| sentence-transformers | ❌ 无平替 | **保留 Python** |
| sounddevice / soundfile | ❌ 无平替 | **保留 Python** |
| gsv-tts-lite | ❌ 无平替 | **保留 Python** |
| pillow | `sharp` | 平替 |
| playwright | `playwright` npm | 平替 |
| maim-message | ❌ 自研协议 | 需按协议重实现或保留 Python |
| maibot-plugin-sdk | ❌ Python SDK | 插件生态红线，见 §5 |
| msgpack | `@msgpack/msgpack` | 平替 |
| Babel | `i18next` | 平替 |

---

## 2. 目录结构

### 2.1 项目根（`tree -L 3` 视角，已剔除 `.venv` / `__pycache__` / `.git` / 缓存）

```text
e:\AI\直播\
├── bot.py                       # ★ 主入口（进程启动、信号处理、重启退出码 42）
├── pyproject.toml               # 依赖 + ruff 配置
├── requirements.txt
├── config/                      # 用户配置（TOML）
│   ├── bot_config.toml          # 主配置（端口、webui、平台、行为）
│   ├── model_config.toml        # 模型与 API Provider
│   ├── bilibili_live.toml       # B 站直播
│   └── tts_config.toml          # TTS
├── data/                        # 运行时数据
│   ├── MaiBot.db (+ -shm / -wal)  # ★ 主库（SQLite WAL）
│   ├── local_store.json
│   ├── webui.json
│   ├── update_notice_state.json
│   └── avatar/qq/...
├── locales/                     # i18n：en-US / ja-JP / ko / zh-CN
├── prompts/                     # 提示词：en-US / ja-JP / zh-CN
│   ├── maisaka_chat.prompt
│   ├── learn_style.prompt
│   └── learn_jargon.prompt
├── plugins/                     # ★ 插件目录（运行时动态加载）
│   ├── ai_drawing/              # AI 画画
│   ├── world-asr/               # 语音识别
│   ├── world-bilibili/          # B 站直播
│   ├── world-minecraft/         # MC 桥接（bridge.py + Node mineflayer）
│   ├── world-pvz/               # 植物大战僵尸（input / vision）
│   └── mineflayer-4.39.0/       # Node 依赖（vendored）
├── tts_service/                 # ★ 独立 TTS 微服务
│   ├── app.py                   # FastAPI，GET /health、GET /tts/stream（SSE）
│   ├── requirements.txt
│   └── refs/*.wav               # 参考音频（音色克隆）
├── dashboard/                   # ★ 前端（React + Vite + Electron）
│   ├── src/{components,hooks,i18n,lib,routes,types}
│   ├── electron/
│   ├── dist/
│   └── package.json             # name: maibot-dashboard, version 1.7.4
├── Cortico-main/                # 独立的 TS 机器人运行时（无关主链路）
├── changelogs/
└── src/                         # ★ 后端源码（见 2.2）
```

### 2.2 `src/` 分层

```text
src/
├── main.py                      # 业务初始化（被 bot.py 调用）
├── chat/                        # 消息接收与回复
│   ├── message_receive/         # bot.py / message.py（消息解析）
│   ├── replyer/                 # 回复生成（含 expression_vector_index.py ★向量）
│   ├── image_system/            # 图片处理
│   └── utils/                   # typo_generator / statistic / timer_calculator
├── cli/                         # 控制台与 CLI（console / maisaka_cli / input_reader）
├── common/                      # ★ 通用基础设施
│   ├── database/                # database.py / database_model.py / migrations/
│   ├── data_models/
│   ├── i18n/
│   ├── message_server/          # api.py / server.py（maim_message 服务端）
│   ├── utils/                   # 图片/语音/消息/会话/配置等工具
│   ├── logger.py                # structlog 封装 + WS 广播桥
│   ├── message_repository.py
│   └── prompt_i18n.py
├── config/                      # 配置加载 / 升级钩子 / 文件监听 / 官方默认值
├── core/                        # event_bus / tooling / announcement_manager / local_operator
├── learners/                    # 表达 / 黑话 / 行为学习器
├── llm_models/                  # ★ LLM 客户端层
│   ├── model_client/            # openai_client / openai_responses_client / gemini_client
│   ├── openai_compat.py         # OpenAI 兼容配置构建
│   └── utils_model.py
├── mcp_module/                  # MCP 客户端
├── maisaka/                     # ★ 核心对话引擎
│   ├── builtin_tool/            # reply / wait
│   ├── context/                 # history / messages
│   ├── focus/                   # 注意力焦点管理
│   ├── memory/                  # mid_term 中期记忆
│   ├── tts/player.py            # ★ TTS 播放（SSE + sounddevice）
│   ├── chat_loop_service.py
│   ├── attention_drift.py / idle_backoff.py / mode_policy.py
│   └── live_text_filter.py
├── plugin_runtime/              # ★ 插件运行时（见 §5）
│   ├── host/                    # supervisor / rpc_server / hook_dispatcher / registry
│   ├── runner/                  # runner_main / plugin_loader / manifest_validator
│   ├── transport/               # uds / tcp / named_pipe / factory
│   ├── protocol/                # envelope / codec / errors
│   ├── capabilities/            # core / data / worlds / components / render / registry
│   └── local_sdk.py
├── plugins/built_in/            # 内置插件（plugin_management / ...）
├── services/                    # ★ 业务服务层（statistics / database / image / memory_flow）
├── worlds/                      # ★ World 抽象（manager.py / bus.py）
├── A_memorix/                   # ★ 长期记忆插件（最大单体，独立 vectors + sqlite）
│   ├── core/embedding/          # manager.py / api_adapter.py（sentence-transformers）
│   ├── core/storage/            # vector_store.py(FAISS) / graph_store.py / metadata_*.py
│   ├── core/retrieval/          # dual_path / sparse_bm25 / pagerank / graph_relation_recall
│   ├── core/runtime/services/   # 30+ admin service（对应 memory 路由）
│   ├── core/strategies/         # chat_log / factual / narrative / quote
│   ├── host_service.py / paths.py / plugin.py
│   └── requirements.txt
└── webui/                       # ★ WebUI 后端（FastAPI）
    ├── app.py                   # ★ 应用工厂
    ├── webui_server.py          # 服务启动
    ├── routes.py                # ★ 主路由聚合，prefix=/api/webui
    ├── dependencies.py          # require_auth
    ├── logs_ws.py               # 日志 WS（当前未注册）
    ├── core/                    # auth / rate_limiter / security
    ├── middleware/              # anti_crawler
    ├── schemas/                 # auth / chat / plugin / statistics
    ├── services/                # ai_search_* / git_mirror
    ├── utils/
    └── routers/
        ├── __init__.py          # ★ get_all_routers()
        ├── config.py  person.py  expression.py  jargon.py  behavior.py
        ├── memory.py  model.py  mcp.py  statistics.py  system.py
        ├── worlds.py  avatar.py  user_emoji.py  bot_accounts.py
        ├── bilibili_live.py  data_transfer.py  reasoning_process.py
        ├── reply_effects.py  search.py
        ├── chat/                # routes.py / service.py / serializers.py
        ├── plugin/              # catalog / management / config_routes /
        │                        # icon_routes / runtime_routes / stats_proxy / progress
        └── websocket/           # auth.py / unified.py / manager.py
```

### 2.3 关键入口文件定位

| 用途 | 文件 |
| --- | --- |
| 进程入口 | [bot.py](file:///e:/AI/直播/bot.py) |
| 业务主初始化 | [src/main.py](file:///e:/AI/直播/src/main.py) |
| FastAPI 应用工厂 | [src/webui/app.py](file:///e:/AI/直播/src/webui/app.py)（`create_app()`） |
| 路由聚合 | [src/webui/routes.py](file:///e:/AI/直播/src/webui/routes.py)（`router = APIRouter(prefix="/api/webui")`） |
| **`get_all_routers()`** | [src/webui/routers/__init__.py](file:///e:/AI/直播/src/webui/routers/__init__.py) |
| 插件运行时 Supervisor | [src/plugin_runtime/host/supervisor.py](file:///e:/AI/直播/src/plugin_runtime/host/supervisor.py) |
| 插件子进程入口 | [src/plugin_runtime/runner/runner_main.py](file:///e:/AI/直播/src/plugin_runtime/runner/runner_main.py) |

`get_all_routers()` 实际只返回 4 个路由器：

```python
return [
    main_router,            # /api/webui  （聚合了 22 个子路由）
    config_compat_router,   # /api/config  （兼容层）
    memory_compat_router,   # /api         （兼容层）
    chat_router,            # /api/chat
]
```

⚠️ 注意：`logs_ws.py` 的 `/ws/logs` 与 `plugin/progress.py` 的 `/ws/plugin-progress` **定义了但未在 `app.py` 注册**，属于历史遗留路径；功能已由 `/ws` 统一入口的 `logs:*` / `plugin_progress:*` 订阅承载。

---

## 3. 数据库与持久化方案

### 3.1 逐条回答

| 问题 | 答案 | 证据 |
| --- | --- | --- |
| 关系库？ | **SQLite**，单文件 `data/MaiBot.db` | `DATABASE_URL = f"sqlite:///{_DB_FILE}"` |
| ORM？ | **SQLModel**（SQLAlchemy 2.x 之上），少量裸 SQL | `from sqlmodel import SQLModel, create_engine` + `text(...)` 混用 |
| 独立向量库？ | **FAISS**（`faiss-cpu`），本地文件索引 | `IndexIDMap2` / `IndexFlatIP` / `ScalarQuantizer(QT_8bit)` |
| KV / 缓存？ | **无 Redis**，全内存态 | 全仓库无 redis 引用 |
| 文件型存储？ | 有，多套 | 见 3.4 |
| schema 在哪？ | `src/common/database/database_model.py` + migrations | `table=True` 模型 50+ 个 |

### 3.2 主库（SQLite）

**连接配置**（[database.py](file:///e:/AI/直播/src/common/database/database.py)）：

```python
DATABASE_URL = f"sqlite:///{_DB_FILE}"   # data/MaiBot.db
engine = create_engine(
    DATABASE_URL,
    echo=False,
    connect_args={"check_same_thread": False},
    pool_pre_ping=True,
)
```

**PRAGMA（每个连接生效）**：

```sql
PRAGMA journal_mode=WAL
PRAGMA cache_size=-64000        -- 64MB
PRAGMA foreign_keys=ON
PRAGMA synchronous=NORMAL
PRAGMA busy_timeout=1000        -- 1s
```

**迁移机制**：`create_database_migration_bootstrapper(engine)`（`src/common/database/migrations`），启动时自动执行；另有运行期索引补偿 `ensure_runtime_performance_indexes()`。

**已定义的 ORM 表**（`__tablename__`，来自 [database_model.py](file:///e:/AI/直播/src/common/database/database_model.py)）：

```text
mai_messages                     llm_usage
images                           tool_records
maisaka_monitor_events           maisaka_reply_effects
one_time_maintenance_tasks       statistics_aggregation_cursors
statistics_message_hourly        statistics_tool_hourly
statistics_model_hourly          high_frequency_terms
online_time                      expressions
behavior_experience_paths        behavior_scene_clusters
behavior_scene_tag_clusters      behavior_actions
behavior_outcomes                jargons
binary_data                      person_info
chat_sessions                    bot_platform_accounts
```

> 实际表数量多于上表（含 A_memorix 独立库与部分运行期建表），迁移时**以 `.schema` 导出为准**，不要只看 ORM 定义。

### 3.3 向量存储（两套并存）

**① FAISS 索引 —— `src/A_memorix/core/storage/vector_store.py`**

```python
# 主索引：8bit 标量量化
quantizer = faiss.IndexScalarQuantizer(dim, faiss.ScalarQuantizer.QT_8bit,
                                      faiss.METRIC_INNER_PRODUCT)
self._index = faiss.IndexIDMap2(quantizer)
# 回退索引：训练完成前可检索，避免冷启动无结果
self._fallback_index = faiss.IndexIDMap2(faiss.IndexFlatIP(dim))
```

- 持久化：`faiss.write_index()` / `faiss.read_index()`
- 归一化：`faiss.normalize_L2()`
- 一致性校验：`_faiss_id_multiset()` 比对 id_map，含恢复 journal

**② 表情向量索引 —— `src/chat/replyer/expression_vector_index.py`**

走 `DATABASE_URL`，与主库同源的向量索引路径。

### 3.4 文件型存储清单

| 路径 | 内容 |
| --- | --- |
| `data/MaiBot.db` `-shm` `-wal` | 主库（WAL 模式，**迁移时三件套要一起处理**） |
| `data/webui.json` | WebUI 状态 |
| `data/local_store.json` | 本地 KV |
| `data/update_notice_state.json` | 更新提示状态 |
| `data/avatar/qq/*.jpg` | 头像缓存 |
| `tts_service/refs/*.wav` | TTS 参考音频 |
| `tts_service/models/` | GSV-TTS-Lite 模型（首次运行下载） |
| `logs/app_*.log.jsonl` | 结构化日志（JSONL） |
| A_memorix 数据目录 | 向量索引 + 独立 sqlite + `import_manifest.json` + `dual_ready.json` |
| `local-cache/images` | 图片缓存（system 路由可查/删/预览） |

### 3.5 迁移建议

- **Drizzle + SQLite 是阻力最小路径**，可以复用现有 PRAGMA 与裸 SQL。
- 立刻要导出真实 schema：`sqlite3 data/MaiBot.db ".schema"`，作为 zod/Drizzle 的单一事实来源。
- FAISS 不建议 TS 重写（见 §4）。

---

## 4. AI 能力调用方式

### 4.1 逐条回答

| 问题 | 答案 |
| --- | --- |
| LLM 调用方式？ | **OpenAI 兼容 HTTP API 为主**，另有 Google Gemini 官方 SDK。**不是** vLLM/Ollama/llama.cpp 本地权重 |
| TTS？ | **本地模型**（GSV-TTS-Lite / torch），独立 FastAPI 服务 + SSE 流式；**播放端用 `sounddevice` 直写声卡** |
| Embedding？ | **本地 `sentence-transformers`**（`SentenceTransformer`），另有 `api_adapter.py` 走主程序 Embedding API |
| OpenIE / LPMM？ | 路由存在（`/import/lpmm-openie`、`/import/lpmm-convert`），转换依赖 `networkx` + `pyarrow` + `pandas` |
| Python 独有推理库？ | `faiss-cpu`、`sentence-transformers`、`gsv-tts-lite`（torch）、`sounddevice`、`soundfile` |

### 4.2 LLM 客户端层（可直译）

`src/llm_models/model_client/` 下三套实现：

- `openai_client.py`（Chat Completions）
- `openai_responses_client.py`（Responses API）
- `gemini_client.py`（`google-genai`）

`openai_compat.py` 负责构建兼容配置，支持自定义鉴权头：

```python
if api_provider.auth_type == OpenAICompatibleAuthType.BEARER:
    if auth_header_name != "Authorization" or prefix != "Bearer":
        client_api_key = ""
        default_headers[auth_header_name] = _build_auth_header_value(prefix, api_key)
```

→ **TS 侧用 `openai` npm 官方 SDK 可平替**，注意保留自定义 header / query / body 覆盖能力。

### 4.3 TTS（硬阻塞）

链路：

```text
maisaka 回复文本
  → src/maisaka/tts/player.py
  → HTTP SSE 请求 tts_service:GET /tts/stream
  → gsv_tts.TTS（use_bert=True, models_dir, sovits_cache=[chunk*2, chunk*2+overlap])
  → 逐 chunk 返回 base64 PCM
  → sounddevice 阻塞写入输出设备
```

关键约束：

- 参考音频在 `tts_service/refs/`，上传接口为 `POST /api/webui/config/tts-audio/upload`
- 设备枚举为 `GET /api/webui/config/tts-audio-devices`（**依赖 `sounddevice`，Windows 有 MME/WASAPI 同名设备问题，代码里做了「名称→唯一索引」解析**）
- 模型目录固定 `tts_service/models`（避免落到用户主目录）

→ **TS 无法复现 torch 推理与 WASAPI 设备控制**，建议此服务原样保留，TS 只做 SSE 客户端。

### 4.4 Embedding（硬阻塞）

```python
from sentence_transformers import SentenceTransformer
self._model = SentenceTransformer(self.config.model_path, cache_folder=...)
```

- 支持多线程 batch（`ThreadPoolExecutor`）、embedding 缓存、`normalize`
- `api_adapter.py` 提供「向主程序 Embedding API 请求」的旁路

→ 要么保留 Python embedding 服务，要么换云端 embedding（**会改变向量语义，历史索引需全量重建**）。

### 4.5 OpenIE / LPMM

`/import/lpmm-openie`、`/import/lpmm-convert`、`/import/temporal-backfill` 属**长任务**（见 §6），转换侧依赖 `networkx`/`pyarrow`/`pandas`。

---

# 二、最好有（决定方案是否落地）

## 5. 插件系统机制

### 5.1 以什么形式存在

**目录 + `_manifest.json` + `plugin.py`（导出 `create_plugin`）**，`manifest_version: 2`。

真实样例（[world-pvz/_manifest.json](file:///e:/AI/直播/plugins/world-pvz/_manifest.json)）：

```json
{
  "manifest_version": 2,
  "id": "maibot-live.world-pvz",
  "version": "0.1.0",
  "name": "植物大战僵尸世界",
  "host_application": { "min_version": "1.0.0", "max_version": "1.2.99" },
  "sdk": { "min_version": "2.0.0", "max_version": "2.99.99" },
  "dependencies": [
    { "type": "python_package", "name": "mss", "version_spec": ">=9.0.0" },
    { "type": "python_package", "name": "pywin32", "version_spec": ">=306" },
    { "type": "python_package", "name": "opencv-python-headless", "version_spec": ">=8.0.0" },
    { "type": "python_package", "name": "pydirectinput", "version_spec": ">=1.0.4" }
  ],
  "capabilities": ["world.register", "world.unregister", "world.event"],
  "i18n": { "default_locale": "zh-CN", "supported_locales": ["zh-CN"] }
}
```

典型插件目录：

```text
plugins/world-pvz/
├── _manifest.json      # 元数据 + 依赖 + capabilities
├── config.toml         # 插件配置
├── plugin.py           # 入口，导出 create_plugin
├── input.py
└── vision.py           # OpenCV 模板匹配
```

### 5.2 怎么被发现和加载（**关键：独立进程 + RPC**）

```python
# supervisor.py
self._runner_process = await asyncio.create_subprocess_exec(
    sys.executable, "-m", "src.plugin_runtime.runner.runner_main", ...
)
```

- Host 进程（主程序）通过 `supervisor` 拉起 **Runner 子进程**
- Runner 内 `PluginLoader` 扫描 `plugins/`，做 manifest 校验、依赖拓扑排序、生命周期钩子
- 传输层自动选型：

| 平台 / 地址 | 实现 |
| --- | --- |
| Windows | **Named Pipe**（`\\.\pipe\...`） |
| Linux / macOS | **UDS**（`.sock`） |
| 含 `:` | **TCP** |

```python
# transport/factory.py
if sys.platform != "win32":  return UDSTransportServer(...)
else:                        return NamedPipeTransportServer(pipe_name=...)
```

- 协议：`src/plugin_runtime/protocol/`（`envelope.py` + `codec.py` + `errors.py`）

### 5.3 插件与主程序怎么通信

- **不直接 import 主程序**（新 SDK）。插件侧说："插件通过 SDK 编写，不再 import `src.*`"
- 通过 **capabilities** 暴露能力：`world.register`、`world.unregister`、`world.event`、`worlds`、`data`、`components`、`render` 等
- 通过 **hooks** 注册回调：`hook_catalog.py` / `hook_dispatcher.py` / `hook_spec_registry.py`
- 通过 **components** 注册 UI 组件：`component_registry.py`（对应前端 `runtime/components`、`runtime/home-cards`）
- 旧版兼容：`LegacyPluginAdapter` + 导入钩子，兼容 `src.plugin_system` 老插件

### 5.4 有插件配置 schema 吗

有。`GET /api/webui/plugins/config/{plugin_id}/schema`，schema 来源为 manifest 与插件自带的配置结构；另有 `config/{plugin_id}/raw`（原文 PUT）与 `config/{plugin_id}/reset|toggle`。

### 5.5 对迁移的影响（**重要**）

这其实是**最有利的一条**：插件已经是「独立进程 + 线协议」的架构，理论上 **TS Host 可以直接复用现有 Python Runner 与协议**，插件生态可零改动保留。需要复刻的是：

- `protocol/codec.py` 的序列化格式
- `envelope.py` 的请求/响应封装
- capability / hook / component 的注册与派发语义
- manifest 校验与依赖拓扑排序

---

## 6. 长任务 / 后台任务机制

### 6.1 用的什么

**没有 Celery、没有 APScheduler、没有 Redis 队列。** 全部是原生 asyncio：

| 模式 | 出现位置 |
| --- | --- |
| `asyncio.create_task(...)` | 最普遍（learners、event_bus、llm 并发、file_watcher、image_manager、mcp_module…） |
| `asyncio.Queue` + worker loop | `src/services/memory_flow_service.py`（`maxsize=256`，命名任务如 `A_Memorix.person_fact_writeback`） |
| `ThreadPoolExecutor` | `src/chat/utils/statistic.py`、A_memorix embedding batch |
| 子进程 | 插件 Runner（§5） |
| 独立服务 | `tts_service`（独立 FastAPI） |

### 6.2 任务状态存哪

**内存字典**，进程重启即丢。示例（[data_transfer.py](file:///e:/AI/直播/src/webui/routers/data_transfer.py)）：

```python
_jobs: dict[str, _TransferJob] = {}
# status: "pending" | "running" | "completed" | "failed" | "cancelled"
```

同类模式见 `retrieval_tuning/tasks`、`import/tasks`、`episodes/*`。**无跨进程共享、无持久化**。

### 6.3 有没有进度上报

有。WS 域 `plugin_progress:main`，以及插件安装/克隆的 `update_progress_callback`（`git_mirror_service`）。

### 6.4 迁移影响

- **单实例假设强耦合**：job 状态在内存 → TS 若做多实例/水平扩展，必须引入 Redis 或 DB 存 job 状态，**这是架构变更，不是平替**。
- 建议：TS 侧用 **BullMQ（需 Redis）** 或 **DB-backed job 表**，二选一取决于是否允许多实例。

---

## 7. WebSocket 实现

### 7.1 用的什么库

**FastAPI 原生 WebSocket**，无 socket.io。统一入口 [unified.py](file:///e:/AI/直播/src/webui/routers/websocket/unified.py)：`@router.websocket("/ws")`。

### 7.2 `/ws` 承载的消息类型

采用 **`domain:topic` 订阅模型**（`src/webui/routers/websocket/manager.py`）：

| domain | topic | 内容 |
| --- | --- | --- |
| `logs` | `main` | 实时日志（含历史回放 `load_recent_logs`） |
| `plugin_progress` | `main` | 插件安装/克隆进度 |
| `maisaka_monitor` | `main` | 对话监控事件（含媒体 `media/{kind}/{hash}`） |

客户端操作：`subscribe` / `unsubscribe`；另有心跳 `ping` → `pong`，以及 `update_nickname` 等会话操作，并支持 `register_chat_session` 绑定会话。

### 7.3 鉴权具体怎么做

```text
1. 浏览器带 Cookie(maibot_session)
2. GET /api/webui/ws-token
   → 校验 Cookie → 生成一次性临时 token
3. 用该 token 建连 ws://host/ws?token=xxx
4. 服务端 verify_ws_token()：校验存在 → 校验未过期 → 校验关联 session 仍有效 → 删除（一次性消费）
```

- 临时 token 有效期 **60 秒**，`secrets.token_urlsafe(32)`
- 存储为**进程内字典** `_ws_temp_tokens`（多实例需换共享存储）
- WebSocket 侧另有 **Cookie 回退**（`websocket.cookies.get("maibot_session")`）
- 未认证时 `/ws-token` 返回 **200 + `success=False`**（刻意为之，避免前端因 401 刷新页面）

### 7.4 房间/频道概念

有，即 `domain:topic` 订阅键（`f"{domain}:{topic}"`），`broadcast_to_topic(domain, topic, event, data)` 定向广播。**不是 socket.io 的 room，是自建订阅表。**

---

## 8. 前端调用情况

### 8.1 前端是否已按 `/api/webui/*` 写死

**是。** `dashboard/src` 中 `/api/webui/` 出现 **109 次，覆盖 40 个文件**，包括 `system-api.ts`（18 次）、`config-api.ts`、`memory-api.ts`、`plugin-api/*`、`auth.ts` 等。

### 8.2 蛇形旧接口还有谁在调

- `dashboard/src` 中 **仅 `lib/http/__tests__/client.test.ts` 引用 `/api/config`**（测试用例，非运行路径）
- 结论：**旧兼容层（`/api`、`/api/config`）当前无第一方调用方**，属于给旧版 Dashboard / 第三方的兼容尾巴。可直接向用户确认后砍掉。

### 8.3 前端项目在不在这个仓库里

**在**，即 `dashboard/`：

- `name: maibot-dashboard`，`version: 1.7.4`（与 `requirements.txt` 中 `maibot-dashboard==1.7.4` 对应）
- React + Vite + TypeScript + Electron（`electron-vite`）
- 同时以 pip 包形式发布，后端从 `dist/` 提供静态资源
- 路径解析逻辑在 [api-base.ts](file:///e:/AI/直播/dashboard/src/lib/api-base.ts)：Electron 用主进程配置的后端地址；浏览器 DEV/PROD 都用同源 + 空 base（Vite proxy / 同源部署）

⚠️ 版本兼容检查：后端有 `GET /api/webui/version-compatibility`，会比对 WebUI 版本与主程序 `pyproject.toml` 声明版本，不匹配会告警（`webui_outdated` / `main_program_outdated`）。

---

# 三、有则更准（加分项）

## 9. 关键模块实现抽样

### 9.1 典型 router

- [src/webui/routers/config.py](file:///e:/AI/直播/src/webui/routers/config.py)（2600+ 行，含提示词版本管理、schema、TTS 上传）
- [src/webui/routers/chat/routes.py](file:///e:/AI/直播/src/webui/routers/chat/routes.py)（独立 prefix `/api/chat`，会话/学习/适配器策略）

### 9.2 向量与图谱

- [src/A_memorix/core/storage/vector_store.py](file:///e:/AI/直播/src/A_memorix/core/storage/vector_store.py)（2700+ 行，FAISS 量化索引 + 恢复 journal）
- [src/A_memorix/core/storage/graph_store.py](file:///e:/AI/直播/src/A_memorix/core/storage/graph_store.py)
- [src/A_memorix/core/retrieval/dual_path.py](file:///e:/AI/直播/src/A_memorix/core/retrieval/dual_path.py)（双路检索：稠密 + `sparse_bm25`）
- [src/A_memorix/core/embedding/manager.py](file:///e:/AI/直播/src/A_memorix/core/embedding/manager.py)

### 9.3 插件加载器

- [src/plugin_runtime/runner/plugin_loader.py](file:///e:/AI/直播/src/plugin_runtime/runner/plugin_loader.py)
- [src/plugin_runtime/host/supervisor.py](file:///e:/AI/直播/src/plugin_runtime/host/supervisor.py)
- [src/plugin_runtime/transport/factory.py](file:///e:/AI/直播/src/plugin_runtime/transport/factory.py)

### 9.4 主入口

- [bot.py](file:///e:/AI/直播/bot.py)（信号处理、`RESTART_EXIT_CODE = 42`、Worker 进程判定 `MAIBOT_WORKER_PROCESS`）
- [src/webui/routers/__init__.py](file:///e:/AI/直播/src/webui/routers/__init__.py)（`get_all_routers()`）

---

## 10. 部署与运行环境

| 项 | 现状 |
| --- | --- |
| Python 版本 | `pyproject.toml` 声明 `>=3.12`；`.venv` 中同时存在 `cpython-311` 与 `cpython-312` 的 `.pyc`，说明混用过 |
| Node 版本 | 仓库根**未发现** `.nvmrc` / `engines` 声明；前端用 Vite + electron-vite |
| 部署方式 | 仓库根**未发现** Dockerfile / docker-compose / systemd / nginx / Caddyfile / `.bat` / `.sh`。属**裸机直接跑 `bot.py`** 模式 |
| 多实例 | **否，强单实例**：job 状态在内存、WS 订阅表在内存、WS 临时 token 在内存、SQLite 单文件 |
| 反向代理 | 未见配置。但代码已按反代场景设计（CORS 白名单含 `:5173` / `:7999` / `:<port>`，Cookie 为 HttpOnly，WS 走同源） |
| 端口 | WebUI `8001`（默认绑定 `127.0.0.1` + `::1`）；maim_message 旧版 WS `8000`；新版 API Server `8090`（**默认关闭**） |
| 重启机制 | 进程退出码 `42` 触发自重启 |
| 外部适配器 | `maim-message==0.6.8` 协议，`MessageServer` + 可选 `WebSocketServer`（`on_auth` / `on_message` 回调，含 `platform_map` 路由缓存） |
| TLS | 见 `dashboard/docs/webui-tls-ssl.md`（仓库内文档） |

---

## 11. 迁移目标与边界（待你决策）

以下是**决策项，不是资料**。我按选项列出，每条都标注了对上面技术事实的影响。

### 11.1 全量重写 vs 混合架构

| 方案 | 说明 | 代价 |
| --- | --- | --- |
| A. 全量 TS 重写 | 需在 TS 复刻 FAISS 检索、sentence-transformers 嵌入、torch TTS、WASAPI 播放、插件 Runner 协议 | **极高**，且 4 项基本不可行 |
| B. TS 网关 + Python 内核（**推荐**） | TS 接管 HTTP/WS/鉴权/前端交互；Python 保留 AI 推理、向量、插件 Runner、TTS | 需定义清晰的服务边界与内网协议 |
| C. 仅前端 + 部分路由 TS 化 | 只重写 WebUI 后端中无 AI 依赖的部分（config/person/jargon/expression/system/plugin 管理） | 收益有限，但风险最低 |

### 11.2 前端能不能改

- 现状：前端**在本仓库**，已 100% 使用 `/api/webui/*` → **接口路径可以改，只要同步改前端**。
- 若外部有第三方 Dashboard / 客户端，则 `/api/webui/*` 属于红线。

### 11.3 数据库能不能动

- SQLite → 其它关系库：**可以**，SQLModel 抽象层较薄，但要处理 PRAGMA（WAL、foreign_keys）与裸 SQL。
- **红线**：`data/MaiBot.db` 是用户数据，迁移必须提供可回滚的 schema 版本号（现有 `schema_migrations` 表机制需保留等价能力）。
- FAISS 索引文件**不建议换格式**，否则需全量重算向量。

### 11.4 时间 / 人力约束

未知，需你补充。

### 11.5 迁移后必须不变的「红线」（建议）

| 红线 | 理由 |
| --- | --- |
| **插件生态**（`_manifest.json` + Runner 协议 + SDK 版本区间） | 已有 `world-*` 等真实插件，破坏即生态归零 |
| **用户数据文件格式**（`MaiBot.db`、`config/*.toml`、A_memorix 数据目录） | 就地升级必须无损 |
| **认证语义**（HttpOnly Cookie `maibot_session` + WS 一次性临时 token） | 前端与 Electron 已按此实现 |
| **LLM Provider 配置格式**（`model_config.toml`，含自定义 header/query/body） | 用户已有大量配置 |
| `/api/webui/*` 路径 | 前端已写死 109 处 |

### 11.6 可以放弃的（建议）

- `/api`、`/api/config` 蛇形兼容层（**第一方已无调用**）
- `/ws/logs`、`/ws/plugin-progress`（**已定义未注册**，功能由 `/ws` 承载）

---

# 附：迁移工作分解建议（基于以上事实）

| 阶段 | 内容 | 阻塞点 |
| --- | --- | --- |
| 0 | 导出真实 schema（`.schema`）、清点 config 全部字段、抓 `/ws` 全量消息样例 | 无（schema 已导出→附录 A，WS 协议已提取→附录 B） |
| 1 | TS 侧 Fastify 骨架 + zod schema + Drizzle + `/api/webui` 路由映射 + Cookie/WS 鉴权 | 无 |
| 2 | 无 AI 依赖的路由迁移（config/person/jargon/expression/behavior/system/plugin 管理/统计） | 无 |
| 3 | WS 统一网关（domain:topic 订阅模型 + 临时 token） | 无 |
| 4 | AI 能力对接：LLM 走 TS SDK；**embedding / FAISS / TTS / 插件 Runner 保留 Python 内网服务** | 需定义内网协议 |
| 5 | 长任务改造：内存 job → DB/Redis job | 需先定「是否多实例」 |
| 6 | 前端联调（Electron + 浏览器双模式）、版本兼容检查 | 无 |

**最小可行红线**：阶段 4 的内网协议一旦定错，后期返工成本最高，建议**优先设计**。

---

# 附录 A：真实 SQLite Schema 导出（2026-09-18，`data/MaiBot.db`）

> 导出方式：`sqlite_master` 全量查询。与 §3.2 ORM 定义**逐一比对一致**——代码即真相，无漂移。

## A.1 表清单（24 张）

| 表 | 主键 | 唯一约束 | 说明 |
| --- | --- | --- | --- |
| `mai_messages` | id | — | 消息主表；`raw_content BLOB`、`platform+message_id` 复合索引 |
| `llm_usage` | id | — | LLM 用量（含 prompt cache 三字段、cost） |
| `images` | id | `image_hash` 索引 | 图片注册表 |
| `tool_records` | id | — | 工具调用记录 |
| `maisaka_monitor_events` | event_id | — | 监控事件流（`payload_json TEXT`，支持 `since_event_id` 回放） |
| `maisaka_reply_effects` | effect_id(VARCHAR 36) | — | 回复效果评分（`request/prompt_fingerprint` 各有索引） |
| `one_time_maintenance_tasks` | task_name | — | 一次性维护任务（含 cursor 游标） |
| `statistics_aggregation_cursors` | source_name | — | 聚合游标 |
| `statistics_message_hourly` | id | (bucket_time, chat_id) | 小时消息统计 |
| `statistics_model_hourly` | id | (bucket_time, request_type, model_name, provider_name) | 小时模型统计 |
| `statistics_tool_hourly` | id | (bucket_time, tool_name) | 小时工具统计 |
| `high_frequency_terms` | id | (chat_id, term) | 高频词 |
| `online_time` | id | — | 在线时长 |
| `expressions` | id | — | 表达方式 |
| `behavior_actions` | id | `action_hash` 索引 | 行为记录 |
| `behavior_experience_paths` | id | `session_id+enabled` 复合索引 | 经验路径 |
| `behavior_outcomes` | id | `outcome_hash` 索引；`session_id+outcome_hash` UNIQUE | 行为结果 |
| `behavior_scene_clusters` | id | — | 场景聚类（`tag_distribution TEXT`） |
| `behavior_scene_tag_clusters` | id | (tag_kind, tag) | 标签聚类 |
| `jargons` | id | — | 黑话/术语 |
| `binary_data` | id | `data_hash` 索引 | 二进制指针（hash→full_path） |
| `person_info` | id | **`person_id` UNIQUE INDEX** | 人物档案 |
| `chat_sessions` | id | **`session_id` UNIQUE INDEX** | 会话索引 |
| `bot_platform_accounts` | id | (platform, account_id) | 平台账号 |

## A.2 关键发现（直接影响迁移设计）

1. **全库零外键**。`PRAGMA foreign_keys=ON` 实际是空跑——所有表间关系（如 `mai_messages.session_id → chat_sessions.session_id`）靠**应用层维护**。TS 侧用 Drizzle 时同样不必声明 FK 引用，可 1:1 平移，不会出现约束冲突。
2. **索引共 90 个**，全部是 SQLModel `Field(index=True)` 生成的命名索引（`ix_*`）；另有 `ensure_runtime_performance_indexes()` 在运行期动态补偿，迁移后此机制需保留（启动时 CREATE INDEX IF NOT EXISTS）。
3. **时间列全是 SQLite DATETIME**（ISO 字符串），无 epoch 整数列。Drizzle 侧用 `text("timestamp")` + ISO 字符串最省事；若改 `integer({mode:"timestamp_ms"})` 需全量数据转换。
4. **两个 BLOB 列**：`mai_messages.raw_content`、`maisaka_reply_effects.record_blob`——Drizzle 用 `blob` 模式 `Buffer`。
5. **`VARCHAR` 无长度语义**（SQLite 特性），TS 侧 zod 校验长度时以 ORM 定义为准（如 `session_id VARCHAR(255)`）。
6. 库中**无** A_memorix 的表（其独立 sqlite + FAISS 文件，见 §3.3/§3.4），主库迁移不含记忆系统。

---

# 附录 B：`/ws` 统一协议规范（静态提取自 [unified.py](file:///e:/AI/直播/src/webui/routers/websocket/unified.py) + [manager.py](file:///e:/AI/直播/src/webui/routers/websocket/manager.py)）

## B.1 建连与鉴权

```text
ws://host/ws?token=<一次性临时token>     # token: GET /api/webui/ws-token 获取，60s 有效，一次性
    或
Cookie: maibot_session=<session>         # 回退路径
```

- 认证失败：服务端主动 `close(code=4001, reason="认证失败，请重新登录")`
- 认证成功后服务端立即下发：`{"op":"event","domain":"system","event":"ready","data":{"connection_id":"<uuid hex>","timestamp":<float>}}`
- 连接断开时服务端自动清理该连接全部订阅与 chat 会话

## B.2 客户端 → 服务端（4 种 op）

所有消息必须是 JSON 对象，公共字段 `op`，可选 `id`（请求 ID，服务端在 response 中原样回传）。

**① ping**

```json
{ "op": "ping" }
```

**② subscribe**（`domain:topic` 只支持下表三种组合）

| domain:topic | data 参数 | 参数约束 | 订阅成功后的快照下发 |
| --- | --- | --- | --- |
| `logs:main` | `replay` | int，0–500，默认 100 | `event:"snapshot"`，`data:{entries:[...]}`（历史日志） |
| `plugin_progress:main` | 无 | — | `event:"snapshot"`，`data:{progress:{...}}` |
| `maisaka_monitor:main` | `since_event_id`、`replay_limit` | int；replay_limit 1–10000，默认 1000 | 逐条回放历史事件，最后补发 `event:"stage.snapshot"`，`data:{entries:[...],timestamp}` |

```json
{ "op": "subscribe", "id": "r1", "domain": "logs", "topic": "main", "data": { "replay": 200 } }
```

**③ unsubscribe**

```json
{ "op": "unsubscribe", "id": "r2", "domain": "logs", "topic": "main" }
```

缺 domain/topic → `invalid_unsubscribe`；退订不存在的订阅不报错。

**④ call**（当前仅 `domain:"chat"`）

| method | 必填 | data 字段 | 成功 response.data |
| --- | --- | --- | --- |
| `session.open` | `session` | `user_id`、`user_name`、`client`、`platform`、`person_id`、`group_name`、`group_id`、`restore` | `{session, session_id:"<connId>:<session>", client_mode, client}`，随后下发初始聊天状态 |
| `session.close` | `session` | — | `{session}` |
| `message.send` | `session` | `content`、`images[]`、`emojis[]`、`files[]`、`voices[]`、`user_name` | `{accepted:true, session}`（处理在后台异步进行） |
| `session.update_nickname` | `session` | `user_name` | `{session, user_name}` |

```json
{ "op": "call", "id": "r3", "domain": "chat", "method": "message.send",
  "session": "s1", "data": { "content": "你好", "images": [], "user_name": "阿明" } }
```

## B.3 服务端 → 客户端（3 种 op）

**response**（对应客户端请求，`id` 回传）：

```json
{ "op": "response", "id": "r1", "ok": true,  "data": { ... } }
{ "op": "response", "id": "r2", "ok": false, "error": { "code": "...", "message": "..." } }
```

**event**（`session`/`topic` 仅在相关时出现；chat 域的 `event` 名取自内部消息 `type`，如 `"message"`）：

```json
{ "op": "event", "domain": "logs", "topic": "main", "event": "snapshot", "data": { ... } }
{ "op": "event", "domain": "chat", "session": "s1", "event": "message", "data": { ... } }
{ "op": "event", "domain": "system", "event": "ready", "data": { ... } }
```

**pong**：

```json
{ "op": "pong", "ts": 1726600000.123 }
```

## B.4 错误码全集（8 个）

| code | 触发场景 |
| --- | --- |
| `invalid_message` | 收到的不是 JSON 对象 |
| `unsupported_operation` | op 不是 ping/subscribe/unsubscribe/call |
| `unsupported_domain` | call 的 domain 不是 chat |
| `unsupported_method` | chat method 不在 4 个之内 |
| `unsupported_subscription` | domain:topic 不在 3 个之内 |
| `invalid_unsubscribe` | unsubscribe 缺 domain/topic |
| `missing_session` | session.open 缺 session |
| `session_not_found` | session 未打开（close/send/update_nickname 时） |

## B.5 TS 实现要点

- 下行有 `enqueue` 队列缓冲（[manager.py](file:///e:/AI/直播/src/webui/routers/websocket/manager.py)），非直接 send——TS 侧需同样实现 per-connection 发送队列，避免背压阻塞事件循环
- `message.send` 处理是 `asyncio.create_task` 后台化——TS 侧用 `void process()` + 错误兜底
- 会话 ID 规则 `"<connection_id>:<client_session>"`，同一连接可开多个逻辑会话
- 本协议无版本号字段，TS 重写时**字段名必须逐字对齐**（前端 dashboard 已按此解析）
