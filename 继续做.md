# 继续做

> 上次工作截止时的状态快照。全部可编程的基础设施已落地并验证通过，
> 剩余工作按优先级排列。接手者按顺序推进即可。

---

## 当前状态

- **仓库**：https://github.com/siaoic/E.V（分支 main）
- **测试**：vitest 90 + pytest 23 = 113 项全绿
- **联调**：TS 服务器已连接真实 `data/MaiBot.db`（1987 条人物 / 736 条黑话）和真实 `data/webui.json` token，14 项联调验证全通过

## 已完成（不要重做）

| 层 | 内容 | 文件 |
|---|---|---|
| 鉴权 | Cookie（HttpOnly + webui.json）+ WS 一次性 token + 登录限流 | `server/src/auth/` |
| DB | Drizzle 24 表（真实 DDL 黄金样例）+ 运行期索引 + SQLite DATETIME 工具 | `server/src/db/` |
| HTTP 路由 | health / auth / person / jargon / statistics / config（读+raw 写） / expression（20 端点） / ws-token | `server/src/http/routes/` |
| WS 网关 | 4 op + 3 域 + 8 错误码 + per-connection 队列 + 订阅表 + chat 会话登记 | `server/src/ws/` |
| RunnerHost | Envelope/MsgPack codec（ext 42/43 字节级对齐）+ TCP supervisor + capability 表 | `server/src/kernel/runner-host/` |
| Kernel | HTTP 客户端 + A_memorix / TTS 代理 / chat_bridge Python 服务脚本 | `server/src/kernel/` + `services/` |
| 配置 schema | pydantic dump 脚本 + schema 三端点（bot / model / section） | `server/scripts/dump_config_schema.py` |
| TOML 写入器 | 手术式更新（保留行内注释 + 引号内 # 正确处理） | `server/src/config/toml-writer.ts` |
| LLM 客户端 | 读 model_config.toml → openai SDK（首个 provider + model） | `server/src/llm/client.ts` |
| chat pipeline MVP | 消息入库 + system prompt + LLM 调用 + 回复入库 | `server/src/chat/pipeline.ts` |
| Python 侧新增 | world-asr / world-minecraft / world-pvz 三插件 + world-bilibili 准入预算 + 延迟双视图 | `plugins/` + `src/worlds/` |
| VTuber 桥 | player.py 包络钩子 + src/maisaka/vtuber_bridge/（口型+表情+互斥检查） | `src/maisaka/vtuber_bridge/` |

---

## 未完成工作（按优先级排列）

### 第 1 优先级：WS `message.send` 接入 pipeline

**现状**：WS 网关的 `message.send` 收到消息后只返回 `{accepted: true}` 并记 debug 日志，不调 LLM。

**要做**：在 `src/ws/gateway.ts` 的 `handleChatCall` → `message.send` 分支中，调用 `src/chat/pipeline.ts` 的 `processMessage()` 并把 `result.reply` 作为 chat event 推回前端。

**步骤**：
1. `WsGateway` 构造函数 options 加 `pipeline?: MessagePipeline` 接口
2. `message.send` handler 中：如果 pipeline 存在，调用 `processMessage(db, client, config, {content, userId, userName, sessionId})`
3. 把 `result.reply` 封装为 chat event `{op:"event", domain:"chat", session, event:"message", data:{...}}` 推回
4. pipeline 不存在时保持现有行为（受理 + debug 日志）

**依赖**：需要在 `buildApp` 中初始化 LLM 客户端（`readLlmConfig(rootDir)` + `createLlmClient(config)`）并传入 WsGateway。

**文件**：`server/src/ws/gateway.ts` + `server/src/app.ts`

---

### 第 2 优先级：历史消息读取（raw_content msgpack 解码）

**现状**：`mai_messages.raw_content` 是 msgpack 编码的 BLOB（Python 侧 `MessageRecv` 对象），TS pipeline 暂不解码，因此 LLM prompt 只有 system prompt + 当前消息，没有历史上下文。

**要做**：
1. 用 `@msgpack/msgpack` 解码 `raw_content` BLOB
2. 从解码后的结构中提取 `content` / `user_nickname` / `user_id` 字段
3. 在 pipeline 中读最近 20 条历史并构建对话上下文

**依赖**：`@msgpack/msgpack`（已安装）。

**注意**：Python 侧写库的 raw_content 格式是 msgpack 编码的 `MessageRecv` 对象序列化，需要先在 Python 侧打印一条真实消息的 raw_content 解码结果，确认字段名后再写 TS 解码逻辑。

**文件**：`server/src/chat/pipeline.ts`

---

### 第 3 优先级：maisaka 特性逐步迁 TS

按影响面从小到大排列：

| 特性 | Python 模块 | 预估行数 | 说明 |
|---|---|---|---|
| 频率控制 | `src/maisaka/idle_backoff.py` + `turn_scheduler.py` | ~400 | 控制回复频率，避免刷屏 |
| 人设注入 | `src/config/official_configs.py` PersonalityConfig + prompt 构建 | ~200 | 把 bot 人设注入 system prompt |
| 世界事件 | `src/worlds/` 基座（已在 TS）+ 插件事件注入 | ~500 | 世界事件进 LLM 上下文 |
| 表达学习 | `src/learners/expression_learner.py` | ~800 | 从对话中学习表达方式 |
| 注意力焦点 | `src/maisaka/focus/` | ~1500 | 对话注意力管理 |
| 记忆集成 | `src/maisaka/memory/` + A_memorix 检索 | ~2000 | 中期记忆注入 prompt |

---

### 第 4 优先级：实机验证（需要真实环境）

| 验证项 | 前置条件 | 步骤 |
|---|---|---|
| 真 Python runner 联调 | TS supervisor 启动 + Python venv | `RunnerHost.start()` spawn 真 runner → world-bilibili 加载 → capability 回调 → 弹幕注入 |
| VTS 授权 | VTube Studio 运行中 + Live2D 模型加载 | 启动 `vtuber_bridge` → 首次弹窗点「允许」→ token 写回 → 口型注入 |
| 准入预算 | world-bilibili 插件 + `admission_enabled=true` | 开播后弹幕洪水 → 验证限流生效 |
| 真实开播全链路 | 以上全部 + OBS 推流 | 弹幕 → 准入 → WS chat → maisaka → 回复 → TTS → Live2D |

---

### 第 5 优先级：D1 二期完整范围

以下是「完整迁移 maisaka 到 TS」的模块清单（按依赖顺序排列）：

| 模块 | Python 源 | 预估行数 | 依赖 |
|---|---|---|---|
| 消息接收 | `src/chat/message_receive/` | ~2000 | message_repository |
| maisaka 运行时 | `src/maisaka/runtime.py` + `reasoning_engine.py` | ~3600 | 上行 + LLM |
| 回复生成 | `src/chat/replyer/` | ~1500 | 上行 + LLM |
| 学习器 | `src/learners/` | ~3000 | 上行 + DB |
| World 框架 | `src/worlds/` | ~800 | 已有 TS 侧基座 |
| 消息出站 | `src/chat/utils/` + `src/services/send_service.py` | ~1200 | 上行 |
| LLM 编排 | `src/llm_models/` | ~2700 | openai SDK |

**总计**：~14800 行 Python → TS 迁移。

---

## 已知 bug / 待修复

| # | 问题 | 影响 | 修复建议 |
|---|---|---|---|
| 1 | schema-dump.json 的 `sections` 为空 | `GET /config/schema/section/webui` 返回 404 | 修复 `dump_config_schema.py` 中 `include_nested=False` 导致 sections 被清空的问题 |
| 2 | `check-routes.ts` 的 import 路径 | 只影响开发调试 | 改为相对于 server/ 的路径 |
| 3 | WS `message.send` 的 `images` 字段只传了 URL 数组 | 前端如果传 base64 会很大 | 后续加图片上传端点，改为传 URL |
| 4 | `data/local_store.json` 与 Python 侧并发写入可能冲突 | 统计缓存丢失 | R4 已记录：MaiBot.db 只有 TS 打开；local_store.json 只有 TS 打开；Python 侧如果也要写 local_store 需走 HTTP |

---

## 快速上手命令

```bash
# 跑全部测试
cd server && npx vitest run
# 跑 Python 测试
.venv/Scripts/python.exe -m pytest plugins/world-bilibili/tests src/worlds/tests -q

# 启动 TS 服务器
cd server && npx tsx src/main.ts

# 生成协议黄金样例（Python 侧）
.venv/Scripts/python.exe server/scripts/dump_protocol_fixtures.py

# 生成配置 schema dump
.venv/Scripts/python.exe server/scripts/dump_config_schema.py

# 检查注册路由
cd server && npx tsx scripts/check-routes.ts

# 全链路联调
.venv/Scripts/python.exe server/scripts/verify_integration.py
```
