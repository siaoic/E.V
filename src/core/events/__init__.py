"""消息结构体强类型 Schema（Pydantic v2，环境已随 mcp SDK 依赖安装）。

用途：事件总线（src/core/bus.py）广播的事件载荷统一走这里的模型，
字段自动校验、缺失即抛异常，并为未来 WebUI / 其他前端提供结构化契约。

设计约束：
  - 仅作用于**新增**的事件总线消息，不改动现有 dict 接口（行为 100% 不变）；
  - 消息预留 api_version 字段，为未来接口迭代兼容铺路。
"""
