{
    "mcpServers": {
        "bing-cn-mcp": {
            "command": "npx",
            "args": ["-y", "bing-cn-mcp"],
            "timeout": 120,
            "description": "必应中文搜索：bing_search（联网搜索）、crawl_webpage（抓取网页内容），用于获取实时信息、新闻、资料。"
        }
    }
}
