# 贡献

这个包用 AGPL-3.0-or-later 发布。框架 [Cortico](https://github.com/Pal-AI-Lab/Cortico) 是 MIT,
两者经 HTTP 与扩展契约相连,许可各归各。

## 提 PR 之前

**一条规则**:提交的代码你要能解释清楚,包括用编码代理写的部分。做不到的 PR 会被关掉。

签一份贡献者许可协议(CLA)。第一次提 PR 时机器人会在评论里给出协议与签署方式,
回一句话即可,不用签纸。

CLA 给的是一份宽授权:你保留自己那部分的版权,同时允许本项目以任何许可再授权这些贡献。
没有它,项目以后连改许可都要挨个征求同意。签过之后,你的贡献会以 AGPL-3.0 对所有人发布,
与项目其余部分一致。

只改错别字、注释或文档的 PR 不需要 CLA。

## 收什么

- 运行时、控制台面板、装载线上的缺陷修复,带一个改之前会红的测试
- 与上游能力对齐的适配(TTS server、VTube Studio、对齐器)
- 文档与诊断信息的改进

## 不收什么

- 人格内容(台本、词表、记忆、观众资料)。那是部署侧的东西,不在这个包里,也不在许可覆盖范围内
- 第三方素材(Live2D 模型、参考音频、模型权重)。一律不入库
- 把某一个操作者的配置写死进代码

## 本地

```bash
corepack pnpm install
corepack pnpm build      # 控制台面板产物
corepack pnpm vitest run
```

真机相关的检查要连外网、要显卡,见 `scripts/` 下带 `check-` 前缀的脚本。
