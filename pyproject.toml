[build-system]
requires = ["setuptools>=80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "MaiBot"
version = "1.2.5"
description = "MaiCore 是一个基于大语言模型的可交互智能体"
requires-python = ">=3.12"
dependencies = [
    "Babel>=2.17.0",
    "blivedm @ git+https://github.com/xfgryujk/blivedm",
    "brotlicffi>=0.9.0",
    "aiohttp>=3.14.3",
    "certifi",
    "colorama>=0.4.6",
    "faiss-cpu>=1.11.0",
    "fastapi>=0.116.0",
    "google-genai>=1.39.1",
    "httpx[socks]",
    "jieba>=0.42.1",
    "json-repair>=0.60.1,<0.61",
    "maim-message==0.6.8",
    "maibot-dashboard==1.7.4",
    "maibot-plugin-sdk>=2.8.1",
    "mcp>=1.28.1,<2",
    "msgpack>=1.2.1",
    "numpy>=2.2.6",
    "openai>=1.95.0",
    "pandas>=2.3.1",
    "pillow>=12.3.0",
    "playwright>=1.54.0",
    "pyarrow>=20.0.0",
    "pydantic>=2.11.7",
    "pypinyin>=0.54.0",
    "prompt-toolkit>=3.0.52",
    "python-multipart>=0.0.31",
    "rapidfuzz",
    "rich>=14.0.0",
    "scipy>=1.7.0",
    "sqlalchemy>=2.0.40",
    "sounddevice>=0.5.1",
    "soundfile>=0.14.0",
    "sqlmodel>=0.0.24",
    "structlog>=25.4.0",
    "tomlkit>=0.13.3",
    "typing-extensions",
    "uvicorn>=0.35.0",
    "watchfiles>=1.1.1",
    "ahocorasick-rs>=1.0.3",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff>=0.12.2",
    "zstandard",
]


[tool.uv]
index-strategy = "unsafe-best-match"

# blivedm 1.1.7 把 aiohttp/yarl 等约束在很旧的版本范围，但 MaiBot 需要新的
# aiohttp（其依赖的 yarl>=1.17 同理）；这些过窄约束与运行时实际兼容性不符，
# 这里强制放宽为满足主项目的版本。
override-dependencies = ["aiohttp>=3.12.14", "yarl>=1.17.0"]
constraint-dependencies = [
    "cryptography>=50.0.0",
    "idna>=3.15",
    "pyasn1>=0.6.4",
    "pydantic-settings>=2.14.2",
    "pyjwt>=2.13.0",
    "starlette>=1.3.1,<1.4",
    "urllib3>=2.7.0",
]

[[tool.uv.index]]
name = "tsinghua"
url = "https://pypi.tuna.tsinghua.edu.cn/simple"

[[tool.uv.index]]
name = "aliyun"
url = "https://mirrors.aliyun.com/pypi/simple"

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
default = true

[tool.pytest.ini_options]
pythonpath = [".", "src"]

[tool.ruff]

include = ["*.py"]

# 行长度设置
line-length = 120

[tool.ruff.lint]
fixable = ["ALL"]
unfixable = []

# 如果一个变量的名称以下划线开头，即使它未被使用，也不应该被视为错误或警告。
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

# 启用的规则
select = [
    "E", # pycodestyle 错误
    "F", # pyflakes
    "B", # flake8-bugbear
]

ignore = ["E711", "E501"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Body", "fastapi.File"]

[tool.ruff.lint.per-file-ignores]
"scripts/run_a_memorix_webui_backend.py" = ["E402"]
"src/plugin_runtime/runner/runner_main.py" = ["E402"]

[tool.ruff.format]
docstring-code-format = true
indent-style = "space"


# 使用双引号表示字符串
quote-style = "double"

# 尊重魔法尾随逗号
# 例如：
# items = [
#     "apple",
#     "banana",
#     "cherry",
# ]
skip-magic-trailing-comma = false

# 自动检测合适的换行符
line-ending = "auto"
