# 复制为仓库根 .env 后设置部署根 CORTICO_HOME。
# 优先级:进程环境 > 此文件 > <主仓库根>/deployments/。
# 相对路径按主仓库根解析;未设置覆盖值的 git worktree 使用同一个默认部署根。
# 含空格的路径可用引号包住。
#
# 部署目录包含 deployment.json、config.json、密钥、Memory 和运行数据。
# 目录结构见 docs/deployment.md。
# World 密钥放在 <部署根>/<部署名>/.env,Provider 密钥放在
# <部署根>/providers/<端点名>/.env;也可使用进程环境。此文件不读取密钥。
#
#CORTICO_HOME=D:\cortico-deployments
#CORTICO_HOME=../cortico-deployments
