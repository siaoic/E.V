/** 包入口:默认导出 `WorldDefinition`,加载器按 `cortico.kind === 'world'` 认它。 */
import { EXAMPLE } from './definition.ts';

export default EXAMPLE;
export { EXAMPLE };
export type { ExampleConfigSection } from './config.ts';
